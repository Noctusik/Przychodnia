<Window x:Class="Przychodnia.WyswietlPracownikowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wyświetl Pracowników" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Imię:" Margin="5"/>
            <TextBox Name="txtFilterImie" Width="100" Margin="5"/>
            <TextBlock Text="Nazwisko:" Margin="5"/>
            <TextBox Name="txtFilterNazwisko" Width="100" Margin="5"/>
            <TextBlock Text="Rola:" Margin="5"/>
            <TextBox Name="txtFilterRola" Width="100" Margin="5"/>
            <Button Content="Filtruj" Click="btnFilter_Click" Margin="5"/>
        </StackPanel>
        <DataGrid Name="dataGridPracownicy" AutoGenerateColumns="True" IsReadOnly="True" Grid.Row="1" Margin="10"/>
    </Grid>
</Window>
